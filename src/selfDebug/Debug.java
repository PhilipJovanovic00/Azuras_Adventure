package selfDebug;

public class Debug {



    //if (delta >= 1) {
    //  update();
    //repaint();
    //delta--;
    //drawCount++;

    //this here is for showing the FPS, might be important, if the game is getting really laggy.

    //if (timer >= 1000000000){
    //    System.out.println("FPS: " + drawCount);
    //    drawCount = 0;
    //    timer = 0;


    // For making a rectangle

    //g2.setColor(Color.decode("#ffffff"));
    //g2.fillRect(x, y, gp.tileSize, gp.tileSize);

}
